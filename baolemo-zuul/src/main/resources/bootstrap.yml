server:
  port: 9000
spring:
  application:
    name: baoleme-zuul
  cloud:
    config:
      name: neo-config
      profile: dev
      discovery:
        service-id: baoleme-config
        enabled: true
zuul:
  sensitive-headers:
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka
ribbon:
  ReadTimeOut: 100000
  ConnectTimeOut: 100000